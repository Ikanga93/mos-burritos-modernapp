# Let Nixpacks auto-detect Python and install from requirements.txt
# requirements.txt is symlinked from backend/requirements.txt

[start]
cmd = "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
